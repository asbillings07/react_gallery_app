import React from "react";

const NotFound = () => {
  return (
    <li className="not-found">
      <h2> 404 - Not Found</h2>
      <p>
        You have reached a page that does not exist press one of the buttons
        above to go back to another page.
      </p>
    </li>
  );
};

export default NotFound;
